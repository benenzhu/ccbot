# 对话记录 · ce242b07

- **目录**: `/root`
- **开始**: 2026-08-01 16:40:56
- **来源**: `/root/.claude/projects/-root/ce242b07-39fa-4db0-a8ed-3b7bb032e0fd.jsonl`

---

## 👤 用户

hi你是什么模型?

## 🤖 Claude

你好！我是 Claude Opus 5，Anthropic 的模型，通过 Claude Code（Anthropic 官方的命令行工具）与你对话。

有什么可以帮你的吗？

## 👤 用户

hi

## 🤖 Claude

你好！有什么需要帮忙的吗？比如写代码、debug、看项目文件、跑命令之类的，都可以说。

## 👤 用户

git clone https://huggingface.co/moonshotai/Kimi-K3 git clone一下. 我想学习下模型的结构？

## 🤖 Claude

克隆成功了。权重文件是 LFS 指针（每个 136 字节，没实际下载），但研究结构完全够用 —— config 和建模代码都是真实的。先看核心配置：

·　·　·

配置读到了，很有料。看下还有哪些代码文件（modeling 文件在 auto_map 里被引用了）：

·　·　·

架构很有意思 —— 2.8T 参数、KDA + AttnRes + LatentMoE、原生多模态。先摸清代码骨架：

·　·　·

克隆完成。这个模型架构确实新东西很多。我先把几个核心创新点的代码读一下，再给你讲结构：

·　·　·

KDA 看完了，再看两个更独特的东西：AttnRes（注意力残差）和 LatentMoE：

·　·　·

核心都读完了，再补两块：AttnRes 的辅助函数 + 模型主循环、以及 Gated MLA：

·　·　·

已经 clone 到 `/root/Kimi-K3`，代码全在（权重是 LFS 指针，没实际下载，但学结构够了）。我把核心读完了，下面是这个模型的架构讲解。

## 总览

**Kimi K3**：2.8T 总参数 / 104B 激活，93 层，原生多模态（文本+图像+视频），1M 上下文，MXFP4 量化权重。

```
KimiK3ForConditionalGeneration          (modeling_kimi_k3.py)
├── vision_tower: MoonViT-V2 (401M)     27 层 ViT，3D patch embed（支持视频）
├── mm_projector: PatchMergerMLPV2      1024 → 7168，2×2 patch 合并
└── language_model: KimiLinearModel     (modeling_kimi_linear.py) 93 层
```

## 四个核心创新

### 1. KDA + Gated MLA 混合注意力（3:1）

`config.json:66-168` 里明确列了层分布：第 4/8/12/…/92/93 层是**全注意力（MLA）**共 24 层，其余 **69 层是 KDA**（线性注意力）。3 层线性 + 1 层全注意力循环。

**KDA（Kimi Delta Attention）** `modeling_kimi_linear.py:477` —— 类 Mamba/GDN 的门控 delta rule 线性注意力：
- q/k/v 各过一个 **kernel=4 的短卷积**（`ShortConvolution`, silu）
- 每 head 一个可学习衰减 `A_log`，低秩 `f_a_proj→f_b_proj` 产生**逐通道**门控 g（这是 KDA 相比 GDN 的关键：细粒度到 channel 而非 head）
- `beta` 走 delta rule 更新强度，`gate_lower_bound=-5.0` 防止衰减爆炸
- 输出经 `FusedRMSNormGated`(sigmoid) 门控 —— `use_full_rank_gate: true` 说明这版用了满秩门
- 训练走 `chunk_kda`，解码单 token 走 `fused_recurrent_kda`

**关键收益**：69/93 层不需要 KV cache，只存固定大小的 recurrent state + conv state（`KimiDynamicCache:120` 明确分了 `conv_states`/`recurrent_states` 和 `key_cache`/`value_cache` 两套）。这才是 1M 上下文能跑的原因。

MLA 层还是 DeepSeek 那套低秩 KV（`kv_lora_rank=512`, `q_lora_rank=1536`），但有两个改动：`mla_use_nope=true`（**全注意力层不用 RoPE**，位置信息完全交给 KDA 层的隐式位置编码）+ `mla_use_output_gate=true`。

### 2. AttnRes（注意力残差）—— 最新颖的部分

`modeling_kimi_linear.py:1075` 的 `_apply_attn_res`。标准 Transformer 是 `h = h + f(h)` 一条残差流；K3 改成了**对历史残差做 softmax 加权聚合**：

```python
v = cat(block_residual, prefix_sum)        # (tokens, num_blocks+1, hidden)
k = rmsnorm(v)
scores = (k * (norm.weight * proj.weight)).sum(-1)
probs = softmax(scores)                     # 在"块"维度上 softmax
h = probs @ v                               # 加权平均，而非直接相加
```

每 `attn_res_block_size=12` 层往 `block_residual` 里存一个检查点（93 层 → 约 8 个块），后续层可以**动态地在 softmax 下选择读取哪一层的历史状态**。attn 前、MLP 前、以及最终输出前各做一次（`KimiDecoderLayer._forward_attn_residual:973`、`_apply_output_attn_res:1226`）。

直觉上：这给了深层网络一条"跳读"通路，缓解 93 层深度下残差流被稀释的问题——本质是在深度方向上加了个轻量注意力。

### 3. Stable LatentMoE —— 稀疏度拉到 56×

`KimiSparseMoeBlock:762`。896 个专家选 16 个 + 2 个共享专家。真正的新意在 `routed_expert_hidden_size=3584`：

```
h(7168) → down_proj → 3584 → [16个专家, ffn=3072] → RMSNorm → up_proj → 7168
                                共享专家走全宽 7168 ─────────────┘
```

路由专家在**降维后的 latent 空间**里算，专家参数量直接减半，所以才敢开到 896 个专家。`latent_moe_use_norm=true` 的那个 RMSNorm 是 "Stable" 的来源——聚合后归一化，防止 top-16 求和的方差随专家数漂移。

路由用 sigmoid + `noaux_tc`（DeepSeek-V3 的无辅助损失负载均衡，靠 bias 项纠偏）。

### 4. SiTU-GLU 激活

`SituAndMul:64`：
```python
situ = beta * tanh(gate/beta) * sigmoid(gate)   # beta=4.0
up   = linear_beta * tanh(up/linear_beta)       # linear_beta=25.0
return situ * up
```

就是 SwiGLU 加了双边 tanh 软钳位，且**强制 fp32 计算**。这是为 MXFP4/MXFP8 量化感知训练服务的——把激活值域压在有界区间内，低精度才不炸。

## 想继续深入的话

几个值得对着看的点：
- `KimiMLAAttention:335` —— NoPE 的 MLA 具体怎么实现的
- `KimiMoEGate:666` —— `noaux_tc` 的 bias 纠偏细节
- `modeling_kimi_k3.py:341` `Rope2DPosEmbRepeated` + `MoonVision3dPatchEmbed:286` —— 视觉侧怎么统一处理图像和视频（时间维 patch）
- `README.md` 里有完整的 benchmark 和 vLLM/SGLang 部署说明

想让我深入讲哪块，或者画个更细的数据流图？

## 👤 用户

我想下我在手机上，我会把你的输出转成语音. 你给我用文字或者转成语音友好的方式解释一下KDA吧. 不用代码. 我是一个linear attention小白, 完全没看过.

## 🤖 Claude

好，我用讲课的方式讲，你听着应该顺。

**先说标准注意力在干嘛**

生成第一百个词的时候，模型会回头看前面九十九个词，给每个词打一个相关性分数，然后按分数加权平均，把它们的信息揉成一个向量。这就是注意力。

它的代价是：每生成一个新词，都要和前面所有词逐个比一遍。所以序列越长越慢，而且前面所有词的键和值都得存在显存里，这就是 KV 缓存。一百万上下文的时候，这个缓存能吃掉几十上百 G 显存。这是硬伤。

**线性注意力的核心转念**

这里有一个很关键的视角切换。你不要再把注意力想成"回头查一遍历史"，而是想成"我随身带着一个记忆本"。

每读到一个新词，我就把这个词的信息写进记忆本里。要用的时候，我不翻历史，我只查这个记忆本。

这个记忆本是一个固定大小的矩阵，比如说一百二十八乘一百二十八。不管你读了一千个词还是一百万个词，它永远这么大。

写入的方式很朴素：把这个词的键向量和值向量做外积，得到一个矩阵，加到记忆本上。查询的时候，拿查询向量去乘这个记忆本，就得到了输出。

于是：每个词的计算量恒定，显存恒定，长度翻倍成本只是线性翻倍。这就是"线性"注意力的意思。代价是，你把无损的历史压缩成了一个有损的固定摘要。

**第一个问题：记忆本会糊**

朴素线性注意力有个致命毛病：它只会加，从来不会忘。读了十万个词，就往同一个本子上叠加了十万次，最后全糊在一起，什么都读不出来了。

第一个补丁叫**衰减**。每写入新东西之前，先把整个记忆本乘以一个小于一的系数，比如零点九九。这样老信息会指数级褪色，新信息更清晰。这就是 Mamba 那一类模型的基本思路。

但衰减还是太粗暴，它是无差别地淡化所有东西。有些信息你其实想长期记住。

**第二个补丁：delta rule，也就是"改写"**

这是 KDA 名字里 Delta 的来源，我觉得这是最漂亮的一步。

与其无脑地往本子上叠加，不如先查一下：关于这个键，我本子上原来记的是什么？然后计算一下，我现在想记的新值，和本子上的旧值差多少。最后，只把这个差值按一定强度写进去。

这就从"追加"变成了"改写"。举个例子，前文说"张三住在北京"，后面又说"张三搬到了上海"。朴素叠加会让北京和上海同时糊在记忆里；delta rule 会把北京这条记录擦掉，换成上海。

那个"写入强度"是模型自己算出来的，每个词都不一样。强度大就是彻底覆盖，强度小就是轻微修正，为零就是这个词不值得记，直接跳过。

**那 KDA 到底加了什么**

Kimi Delta Attention 就是在"衰减加改写"这套基础上，做了几处很实在的升级。

第一，也是最关键的一点：**遗忘变成了逐通道的**。前面说的衰减，通常是每个注意力头一个数，整个头一起淡化。KDA 不是，它给记忆本的每一个维度、每一个通道，都单独算一个遗忘率，而且是根据当前这个词的内容动态算出来的。

打个比方，原来是整本笔记本一起泛黄；现在是，记人名的那几页可以保存很久，记临时数字的那几页读完就淡掉。遗忘这件事从一个粗旋钮，变成了几百个细旋钮。这是 KDA 区别于同类方法最主要的地方。

第二，为了不让遗忘失控，它给这个遗忘率加了一个下限。因为如果某一步算出的衰减太狠，记忆会被瞬间清空，训练就崩了。加个地板，稳定性好很多。

第三，在算查询、键、值之前，先过一个宽度为四的小卷积。也就是让每个词先偷看一眼它紧挨着的前三个词。这个成本极低，但对局部模式的捕捉帮助很大，等于把"看邻居"这件廉价的事从注意力身上卸下来了。

第四，输出的时候有一个门控。模型可以逐维度决定，这一步我到底要不要采信从记忆本里读出来的东西。相当于一个音量旋钮。

**为什么还要留下四分之一的标准注意力**

这是我觉得 K3 设计上最务实的地方。

九十三层里，有六十九层是 KDA，另外二十四层是标准的全注意力，每隔三层插一层。

原因是，固定大小的记忆本再聪明，也终究是有损压缩。有些任务你必须精确回忆原文，比如从一百万字里逐字找出某个函数的定义。这种事只有真的回头看原文才靠得住。

所以让绝大多数层用便宜的 KDA 扛住主要的计算量和显存，再留少数几层做精确检索。九十三层里只有二十四层需要 KV 缓存，缓存量直接降到四分之一左右。这就是一百万上下文能跑起来的直接原因。

顺带一提，那些全注意力层是不加位置编码的，位置信息完全靠 KDA 层提供。因为 KDA 天生就是从左往右一个一个读的，顺序信息本来就烙在它的递归过程里了，不需要额外再告诉它谁前谁后。

**一句话总结**

标准注意力是每次都把整本历史重读一遍，准确但越来越贵。KDA 是随身带一个固定大小的记忆本，每读一个词就有选择地改写其中几页，同时让不同的页以不同的速度褪色，所以不管读多长，成本都不变。K3 把这两者按三比一混着用，多数层省钱，少数层保精度。

如果你想再往下听一层，我可以讲讲 delta rule 为什么在数学上其实是在做在线的最小二乘回归，或者讲讲 K3 另外两个更奇特的设计：注意力残差和 LatentMoE。

## 👤 用户

我想下，我们接下来继续语音对话吧。就是语音其实也是能讲更详细的一些，就是我其实想学习一下它推理的代码具体是怎么样，我们可能先不用管训练，对吧？

那普通的 Attention 可能是 Q at K 除以一下 V，然后整体开个根号。不对，不用除 V，最后 at 一下 V，对吧？

那我其实就想知道这个 KDA 它可能在做的操作具体是在做什么呢？然后那个矩阵有多大呢？对，我们一步一步，你来教我，对。当然我现在可能不在电脑前，看不了代码。 我们就只管这个先, 其他的残差和latentmoe我不说你先不用管.

## 🤖 Claude

好，先确认你那句话。标准注意力是：Q 乘 K 的转置，除以根号下每个头的维度，然后过 softmax，最后再乘 V。你自己已经改对了，就是这个。那个根号是除以头维度的平方根，K3 里头维度是 128，所以是除以大约 11 点 3。

现在我们一步一步把 KDA 拆开。我先给你把尺寸定下来，因为有了具体数字，后面每一步你都能在脑子里看见形状。

**尺寸**

K3 的隐藏层宽度是 7168。KDA 有 96 个头，每个头 128 维。96 乘 128 等于 12288，所以 Q、K、V 三个投影都是从 7168 映射到 12288。注意这里没有用 GQA，Q、K、V 都是完完整整的 96 个头，不共享。

然后是最重要的那个东西：**状态矩阵**。每个头有一个，大小是 128 乘 128，也就是一万六千三百八十四个数。

为什么是 128 乘 128？因为它的作用是"从键空间映射到值空间"。键是 128 维，值也是 128 维，所以这个映射矩阵就是 128 乘 128。你可以把它想成一张查找表，你拿一个 128 维的键去问它，它还你一个 128 维的值。

96 个头，每个头一张 128 乘 128 的表，一层就是大约一百五十七万个数。69 个 KDA 层加起来，大概一亿零八百万个数。用 bf16 存，大约两百多兆显存。

**这个数字要品一下**：两百多兆，是固定的。你输入一千个 token 是它，输入一百万个 token 还是它。而如果这 93 层全用传统的多头注意力，一百万上下文的 KV 缓存大概要好几个 TB。这就是差距。

**现在讲解码时，一个 token 进来发生了什么**

第一步，投影。当前 token 的隐藏向量，7168 维，分别乘三个矩阵，得到 Q、K、V，各 12288 维，然后切成 96 个头，每个头 128 维。这一步和标准注意力一模一样。

第二步，短卷积。Q、K、V 各自过一个宽度为 4 的深度可分离卷积。深度可分离的意思是，12288 个通道各算各的，第一个通道只看自己的历史，不和别的通道混。宽度 4 就是当前位置加上前面 3 个位置，四个数加权求和，然后过一个 silu 激活。

所以解码的时候，你要额外存一个小缓冲区，记着前 3 个位置的 Q、K、V 值。这就是代码里说的 conv state。它很小，一层大概十来万个数，相比状态矩阵可以忽略。

第三步，Q 和 K 做 L2 归一化。每个头的那 128 维向量，除以自己的模长，变成单位长度。这一步很关键，后面讲改写的时候你会明白为什么。

第四步，算三个门。这三个门是 KDA 的灵魂，我一个一个说。

**第一个门：遗忘门**。它决定状态矩阵里哪些东西该淡掉。它是这样算的：先用一个 7168 到 128 的矩阵把隐藏向量压到 128 维，再用一个 128 到 12288 的矩阵展开回来。这是个低秩结构，秩只有 128。为什么要低秩？因为如果直接用 7168 乘 12288 的满矩阵，光这一个门就要八千八百万参数，太贵了。低秩之后只要一百八十万左右。

展开之后，每个头拿到 128 个数，也就是**状态矩阵的每一行都有自己独立的遗忘率**。这就是我上次说的"逐通道遗忘"。

然后这 128 个数会和两个东西结合：一个是每个头一个的可学习参数 A，96 个头就 96 个数；还有一个偏置项。最后算出来的衰减系数在 0 到 1 之间。大致形式是取指数衰减，具体的融合公式在 Triton 算子里，不在这个仓库的 Python 代码中，所以细节我没法给你打包票，但结构就是 Mamba 那一类的做法。

另外有个 gate lower bound，值是负 5。它是给对数域的衰减加了个地板，防止某一步算出来的衰减太狠、把状态一下子清零。

**第二个门：写入强度，叫 beta**。它是 7168 映射到 96，也就是**每个头只有一个标量**，然后过 sigmoid，落在 0 到 1 之间。注意它和遗忘门的粒度完全不同：遗忘是每通道一个，写入强度是每头一个。

**第三个门：输出门**。7168 直接映射到 12288，这个是满秩的，配置里那个 use full rank gate 就是这个意思。它不参与状态更新，只在最后决定输出要放多大音量。

第五步，更新状态矩阵。这是整个机制的核心，我用文字把它讲清楚。

对每个头，做三件事：

一，**遗忘**。把状态矩阵的每一行，乘上那一行自己的衰减系数。行对应的是键空间的一个维度。所以效果是：记录某类特征的那些行可以衰减得很慢，记录另一类的可以衰减得很快。整张表不是一起泛黄的，是分行泛黄的。

二，**查旧值**。拿当前这个 K，去问衰减后的状态矩阵："关于这个键，你现在存的值是什么？"具体就是 K 和状态矩阵做一次向量乘矩阵，得到一个 128 维的旧值。

三，**改写**。用真实的 V 减去这个旧值，得到误差。然后做 K 和这个误差的外积，得到一个 128 乘 128 的矩阵，乘上 beta，加到状态上。

这就是 delta rule。我特别想让你注意的是它和朴素做法的区别：朴素线性注意力是直接把 K 和 V 的外积加上去，不管表里原来有什么。delta rule 是先读一下、算个差、再改。所以它是覆盖，不是堆叠。

现在回过头说为什么要 L2 归一化。因为这个更新在数学上等价于对这个键做一步梯度下降，目标是让"查这个键得到 V"这件事成立。步长的合理范围取决于 K 的模长。如果 K 的模长忽大忽小，步长就会失控，训练和推理都不稳。归一化到单位长度之后，beta 等于 1 就恰好是完全覆盖，beta 等于 0 就是完全不动，中间是插值。语义变得非常干净。

第六步，读出。拿 Q 和更新后的状态矩阵做一次向量乘矩阵，得到 128 维的输出。就一次矩阵向量乘法，没有 softmax，没有回头看历史。

第七步，输出门控。这 128 维过一个 RMSNorm，再逐元素乘上输出门的 sigmoid。

第八步，96 个头拼回 12288，过一个 12288 到 7168 的输出投影。结束。

**现在算笔账，这个很有意思**

状态更新那部分，每个头大概三次 128 乘 128 规模的运算，也就是大约五万次乘加。96 个头加起来，六百多万次。

而四个投影矩阵，Q、K、V 加上输出投影，每个都是 7168 乘 12288 这个量级，四个加起来三亿五千万次乘加。

**所以那个看起来很花哨的状态更新，成本还不到投影的百分之二。** KDA 层的开销基本上就等于四个线性层的开销，和标准注意力层是同一个量级。

再对比一下标准注意力在一百万上下文时的读取：每个 token 要和一百万个键做点积，96 个头、128 维，光 QK 就是一百二十多亿次乘加，加上乘 V 再翻一倍。相比 KDA 的六百万，差了大概四千倍。而且它还在随长度继续涨，KDA 是一条水平线。

**最后说一下 prefill 和 decode 的区别**

上面讲的是 decode，一次一个 token，走的是纯递归路径，代码里叫 fused recurrent。

但 prefill 的时候你有几万甚至上百万个 token 同时进来，如果还一个一个递归，GPU 全在空转，因为矩阵向量乘法根本喂不饱它。

所以走另一条路，叫 chunk，分块并行。做法是把序列切成小块，比如 64 个 token 一块。**块内**，用类似标准注意力的方式，直接做矩阵乘矩阵，把这 64 个 token 之间的相互作用一次算完，充分利用 GPU。**块之间**，才用递归的方式传递状态矩阵。

于是一百万个 token 只需要递归一万多步，而不是一百万步，同时每一步内部都是又大又满的矩阵乘法。

这里非常关键的一点是：**两条路径在数学上是等价的**，算出来的结果一样，只是计算顺序不同。代码里的判断就是一行：如果有缓存且当前只有一个 token，走递归；否则走分块。

到这儿 KDA 的推理路径就完整了。

下一步你想听哪个方向？我觉得有三个都挺值得展开：一是 delta rule 为什么等价于在线的最小二乘、也就是它到底在"学"什么；二是分块并行内部是怎么把递归展开成矩阵乘法的，这个技巧本身很巧妙；三是那 24 层 MLA 具体怎么和 KDA 配合，尤其是为什么它们敢完全不用位置编码。

## 👤 用户

我想啊，你知道费曼学习法吗？就是说我们对一个知识足够了解的话，前提是我们能够好好的讲给别人。那这时候你来，我来给你讲一下我理解中的这个 KDA。

首先就是进来以后，它 Projection 那一层应该是和普通的注意力区别不大的。多少个头可能只是训练中的一些配置，我们先不用管。

那首先就是 Q Projection 和 V Projection 后面怎么继续乘来着？我有点。核心思路我听你说应该是我们想要让 KV 先乘，对吧？然后它们怎么乘完以后再和 Q 去乘，对，最后才能够把它更新到一个。那个门控矩阵里，是吧？哎，你可以稍微缩短一点，刚刚那个其实有点太长了，我们来一点一点来理，对。然后你也可以再问我两个问题，我们一起来把这个慢慢搞清楚。对，说的那个 Triton 代码不确定那个，你可以 clone 一下那个仓库，对，那样的话可能会好一点。

## 🤖 Claude

你抓到最关键的那一点了 —— **先算 KV，再让 Q 去乘**。这就是整个线性注意力的命门。我先把 clone 跑上，边聊边看。

·　·　·

仓库在后台拉，我们先聊。

你说的完全对，而且这个直觉是整件事的核心。我来把它讲实一点。

标准注意力是 Q 乘 K 转置，得到一个"每个 token 对每个 token"的大方阵，然后这个方阵再乘 V。序列一百万，那个方阵就是一百万乘一百万，这是灾难。

线性注意力的做法是：**利用矩阵乘法的结合律，把括号换个位置**。原来是 (Q 乘 K转置) 再乘 V，现在改成 Q 乘 (K转置 乘 V)。

关键在于中间那个 K转置乘 V，它的形状是**键的维度乘以值的维度**，也就是 128 乘 128。跟序列多长完全没关系。你把一百万个 token 的 K 和 V 全累加进去，它还是 128 乘 128。

所以你说的"想让 KV 先乘"，就是这个换括号。整个线性注意力就是从这一步长出来的。

现在我问你第一个问题，这个想清楚了后面就顺了：

**既然 K转置乘 V 是把所有 token 累加成一个 128 乘 128 的矩阵，那你觉得，这个累加是怎么做的？换句话说，来了一个新 token，它的 K 和 V 是以什么形式"加"进这个矩阵的？**

提示：K 是一个 128 维向量，V 也是一个 128 维向量，而目标是一个 128 乘 128 的矩阵。两个向量怎么变出一个矩阵来？

（顺带纠正一个小地方：你说"更新到门控矩阵里"—— 那个 128 乘 128 的东西不叫门控矩阵，它叫**状态矩阵**，是记忆本身。门是另外几个小东西，负责控制往这个本子上怎么写、怎么忘、读出来放多大声。这两个概念分开，后面会清楚很多。）

·　·　·

KDA 的内核代码拉到了，`fla/ops/kda/naive.py` 应该有可读的参考实现，等下正好可以用它验证我们推的公式。（顺带一提，同一个仓库里还有 `attnres` —— K3 那个注意力残差也已经进了 fla。）

不过先回到你那边 —— 我的问题还摆着：

**K 是 128 维向量，V 是 128 维向量，怎么变出一个 128 乘 128 的矩阵？**

你先说说想法，或者说"不知道"也行，我给你拆。这一步想通了，delta rule 那部分基本就是白送的。

## 👤 用户

哦，我想啊，这个其实就是我们最简单的说一个一一二八 1128 和一个 1281 的矩阵。哦，我可能说反了，对，这样相乘，也就是我们通常所说外积，然后外积会扩到一个 128×128 的矩阵上。然后，哦，你这里说的怎么门控怎么衰减，我大概理清了，就是它这个我们算出来以后，然后怎么去想加到那个状态矩阵上，其实我们是想要算一个怎么加的那个参数的，对吧？然后具体加多少，什么都得都得由这个东西来怎么样推出来，我说的大概对吗？

## 🤖 Claude

对，完全正确。外积。一个 128 乘 1 的列向量，乘一个 1 乘 128 的行向量，撑开成 128 乘 128 的矩阵。

而且这个矩阵的含义很直白：它就是"如果你拿这个 K 来查，我就还你这个 V"这一条记录，写成矩阵的形式。你把很多条这样的记录叠在一起，就成了一张查找表。之所以能叠而不互相毁掉，是因为不同的 K 方向大致互相垂直，各占各的地盘。

后半句你也说对了，而且说到了点子上：**怎么加、加多少，不是固定的，是模型自己现算出来的。** 那几个门就是干这个的。

那我问下一个，这个是 delta 的核心：

朴素的做法是，来一个 token 就把 K 和 V 的外积直接加到状态矩阵上。现在设想一个情况 ——

**前文说"张三住在北京"，模型把这条记进去了。后面又出现"张三搬到了上海"。这两句里"张三"这个键基本是同一个方向，但值一个是北京、一个是上海。如果还是直接把新的外积加上去，你觉得会发生什么？后面再拿"张三"去查这张表，查出来的是什么？**

## 👤 用户

OK，我想一下，你这个东西大概是说到刚才我们所说的遗忘和那个怎么去相加上了，对，就有一些信息其实它是没有用的，然后我没有必要给它加上去。但是有一些过时的信息，比如张三在上海。不是张张三在北京，这个北京就得我们就得尝试给他忘掉，对。所以我们需要去算那个遗忘门控和那个怎么加对，我想一下，这里其实还有一个问题，就是说我们会去说，其中一个 gate 是整个 128128 一个，对吧？另一个是每 128 个蒂姆就一个，所以对应这样一个。状态矩阵它会有 128 个值来控制，是吧？我想一下，应该是遗忘的那个是 128 个，然后相加的应该是只有一个，然后给它相加上去，对吗？然后最终那个输出矩阵，就是你说多想那个，我其实还没有太理解到，对。这里其实有一个关键，就是我们怎么去理解 KV Cache 是什么东西，对吧？然后 Q 又和它做什么？通常理解来说，K 的作用是说用来表示我们和 Q 有多相关，对吧？然后 V 里面其实是存的真正的知识。然后 QK 以后我们算出来怎么去这种知识里面取，然后去和这个知识乘一下，然后把它取出来。这是我们通常对普通的 Tensor 的理解，对吧？但是 linear Tensor 上这种应该也是一样的。 V 就是那条 128 的向量吗？我想一下，我们是一个自回归，所以我们求出来的 K 和 V 应该都是当前 Token 的。然后当前 Token 乘完以后怎么加上去？怎么感觉哪里有点奇怪呢？你大概能理解吗？我哪里说的有问题？

## 🤖 Claude

我把参考实现拉到了，正好可以拿它对你的话逐条验。

**先说你对的部分。**

门的粒度你完全说对了。遗忘门是 128 个数，对应状态矩阵的 128 行；写入强度 beta 是每个头一个标量。代码里签名写得清清楚楚：`g` 的形状是 `[B, T, HV, K]`，K 就是 128；`beta` 的形状是 `[B, T, HV]`，没有最后那一维，就是一个数。你猜的和代码一模一样。

K 和 V 的分工你也说对了，而且这个理解在线性注意力里**原封不动地成立**。K 是"用什么来查"，V 是"查到了给你什么"。没变。

**现在说你觉得别扭的那个地方 —— 你的直觉是对的，那里确实有个坎。**

你说："当前 token 乘完以后怎么加上去？感觉哪里有点奇怪。"

我猜你卡在这儿：当前 token 自己的 K 和 V 外积加进状态矩阵，然后当前 token 自己的 Q 又去查这个状态矩阵 —— 那不就是自己查自己吗？这有什么意义？

意义在于：**状态矩阵里不只有当前 token，它装着前面所有 token 累积下来的东西。** 当前这个 token 的外积只是最新加进去的一条记录，而 Q 查的时候，查的是这整本累积了几十万条记录的表。所以"自己查自己"只是其中极小的一部分，绝大部分查到的是历史。

你看代码第 61 到 63 行，那个循环体就三行，顺序是：先衰减、再写入、最后读出。

**然后是那个真正的坎 —— 你说"我们是自回归，K 和 V 都是当前 token 的"。**

这句话本身对，但它藏着一个陷阱。我反过来问你：

在标准注意力里，第 100 个 token 要算输出，它拿自己的 Q，去和**前面 99 个 token 各自的 K** 做点积。也就是说，**那 99 个 K 必须原封不动地存着**，这就是 KV cache，这就是为什么它会爆显存。

那么在 KDA 里：状态矩阵是 128 乘 128，固定大小。前面那 99 个 token 的 K 和 V，**一个都没有单独存下来**，全被外积累加压进了这一张表里。

**那我问你：既然那 99 个 K 都不存在了，Q 是怎么还能"查到"某个特定 token 的信息的？这个查询在数学上到底发生了什么？**

再给你一个提示，往这个方向想：Q 乘状态矩阵，而状态矩阵是一堆 K 和 V 的外积之和。你把它展开会看到什么？Q 会先碰到谁？

## 👤 用户

我想一下，大概我们刚才也说了，就是他其实还是 Q 乘 K 乘 V，然后我们先乘的 K 和 V，其实有点类似 MLA 里面我们也做了一个什么矩阵吸收那种东西是吧？我记得它好像也是 Q。啊，不对，他俩也是 K 和 V 先乘的吗？我有点忘了 MLA 里面那个矩阵吸收是怎么做的。那这样 Q 如果去乘那个 latent 矩阵，其实是相当于它还是先和 K 乘，然后再和 V 乘，对吧？然后我们这个矩阵只是给它压在一起了，然后这样能成一个 linear，但是某种程度上它和原来的那个还是一样的吗？对这里，其实对遗忘其实我还有一个问题，那不是会导致 KDA 对长上下文里面越远的地方其实遗忘的越深？那就比如我刚开始我读了一段代码，但其实到了非常长，比如 900 k 那个 token 的时候，其实最前面刚读进来的一些用户的 prompt，那些其实是很重要的东西，对吧？但是它都已经忘掉了。这时候不会产生什么问题吗？我其实一直没有绕过来这个弯，因为我们其实有一个衰减系数会让它一直衰减嘛，但是衰减好像是128 个是吧？里面可能有一个，比暑假的用户最开始的 prompt，然后这个就可以少衰点一点，所以模型其实某种程度上能学到这些东西，是吗？

## 🤖 Claude

你第一段推对了，我把它写实：

状态矩阵是一堆外积之和。Q 乘上去，展开之后就是 —— Q 先和每一个 K 做点积得到一个标量，那个标量再去乘对应的 V，最后全部加起来。**所以 Q 确实还是先碰 K、再碰 V，顺序没变，只是这些点积不再是一个一个显式算出来的，而是被矩阵乘法一次性并行完成了。**

MLA 的矩阵吸收你方向记对了，也是把两个投影矩阵预乘合并，省掉中间那一步。但性质不同：**矩阵吸收是纯粹的等价变形，一个数都不差。而线性注意力的换括号不是等价的。**

**差在哪？差在 softmax。**

标准注意力算完 Q 点 K 之后，要过一次 softmax。softmax 是跨所有 token 归一化的，它做两件事：一是把分数拉成尖的，最相关的那个 token 拿走绝大部分权重；二是保证权重和为一。

而只要你插了 softmax 进去，括号就换不动了 —— 因为 softmax 必须先看到完整的分数向量。**线性注意力能换括号的前提，就是把 softmax 扔掉。**

扔掉的代价就是：检索从"尖锐"变成"模糊"。标准注意力能几乎无损地取出某一个 token；线性注意力取出来的是一个加权混合，相关的重一点、不相关的轻一点，但都在里面。这就是那个有损压缩的来源。

**现在说你那个遗忘的问题 —— 这个问得非常好，你已经摸到设计的要害了。**

你自己给出的猜想基本就是答案，我把它补完整。有三层。

**第一层，衰减率是可以逼近 1 的。** 那 128 个衰减系数是模型自己算的，它完全可以让某些通道每步只衰减万分之一。那这个通道存的东西经过几十万步都还在。而另一些通道可以衰减得很快，专门放临时信息。你说的"有些通道可以少衰减一点"，就是这个意思，说对了。

而且**它是内容相关的** —— 衰减率是当前 token 算出来的。所以模型可以学到：读到无关紧要的内容时，衰减小一点，别把好东西冲掉；读到一段新章节的开头时，某些通道使劲衰减，清场。

代码里还有一处细节印证了这个设计：`g` 在 log 空间累加（`g.cumsum`），而 K3 给它设了个下限 -5。也就是说单步衰减最多把某个通道压到大约百分之零点七，不允许一步清零。它防的是"忘太快"，不是"忘太慢"。

**第二层，也是更重要的一层：delta rule 让"忘"不再只靠衰减。**

这是我想让你抓住的转念。朴素线性注意力只能靠衰减来腾地方，所以它面临的是一个死结：衰减慢就糊掉，衰减快就失忆。

delta rule 换了个思路：**要清除旧信息，不是靠等它褪色，而是靠精确覆盖。** 张三从北京搬到上海，是"张三"这个键上的记录被改写了，和时间过了多久没关系。第一个 token 写进去的东西，只要后面没有任何 token 拿相近的键来覆盖它，它就一直在那儿。

所以遗忘的主线不是"越远越模糊"，而是"**越被后来的内容冲突覆盖，才越模糊**"。这两个是完全不同的机制。你那个九十万 token 之后还记不记得开头 prompt 的担心，在纯衰减模型里是真问题，在 delta rule 下缓解了很多。

**第三层，也是最实在的：K3 根本没打算让 KDA 单独扛这件事。**

九十三层里有二十四层是标准的全注意力，KV cache 一个字都不丢，完整保存一百万 token 的原文。

所以分工是这样的：KDA 负责绝大部分的语义流动和局部推理，成本恒定；那二十四层负责精确回忆 —— 你要从九十万 token 前逐字捞出某个函数签名，靠的就是它们。

**Moonshot 的赌注是：需要逐字精确回忆的场合，比我们想象中少，二十四层够用了。** 这是个工程判断，不是数学保证。而回报是显存降到四分之一左右，一百万上下文才跑得起来。

---

到这儿你其实已经把 KDA 讲通了。我给你留最后一个问题，答上来这块就算你的了：

**KDA 的状态矩阵是 128 乘 128，一万六千多个数，可以粗略看成"最多能存一万六千个数的信息量"。但 K3 的上下文是一百万 token。**

**这个账明显是算不平的 —— 一百万 token 的信息，塞不进一万六千个数。那你觉得，KDA 每一层实际上在"记"的到底是什么东西？为什么这个账算不平，模型却还能工作？**

## 👤 用户

我觉得，可能首先是因为它有很多个不同的头嘛，在每一层。当然每一层不同的头，可能也不一样。那它记录的可能就是一些重要的信息。就比如老师讲一堂课，但是我们其实真正要记的笔记并没有多少，对吧？就比如你刚才给我讲了一大段，其实我记下来的也只有其中的一部分，这样对。我这个理解大概对吗？

还有，你可以顺便给我讲一下，就是那三个门是用什么算出来的？我刚刚有点忘了，就是它们都是用 KV 乘出来那个 128128，然后怎么过一个 GATE 算出来的，是吗？

## 🤖 Claude

你的比喻抓对了核心，而且比你以为的更准。我先补个数字，因为我上一个问题问得有点不厚道 —— 我只说了一万六千个数，那是**单个头**的。

一层有 96 个头，所以一层的记忆预算是一百五十七万个数。69 个 KDA 层加起来大约一亿。摊到一百万 token 上，大概每个 token 能留下一百个数左右。

**所以账其实没那么离谱，但依然是重度压缩** —— 每个 token 进来时是 7168 维，最后在整个 KDA 系统里只留下百来个数的痕迹。你的"记笔记"比喻正好是对的：不是逐字抄写，是提炼。

而且有两点让这个比喻更贴切。

一是**分工**。96 个头，69 层，每个头都有自己那张 128 乘 128 的表，各记各的。有的头可能专门盯代词指向谁，有的盯括号配对，有的盯当前在哪个函数体里。就像一堂课你可能同时记着板书、例题、和老师强调过的考点，分在不同的本子上。

二是**它不用记全**。那二十四层全注意力保存着一字不差的原文。KDA 记的是"要点和线索"，需要原文的时候，另外那套系统去捞。你听我讲课，笔记记要点就行，因为讲义还在。

---

**现在说三个门 —— 这里你有个误解，而且是个挺关键的误解，我们把它掰过来。**

你说"它们是用 KV 乘出来那个 128 乘 128 算出来的"。**不是。三个门和 Q、K、V 一样，全都是直接从这一层的输入向量算出来的。**

也就是说，一个 token 进到这一层，手里就一个 7168 维的向量。然后**并行地**从它身上投影出六样东西：Q、K、V，加上遗忘门、beta、输出门。六个投影同时算，谁也不依赖谁。

算完之后，才拿这六样东西去更新那个 128 乘 128 的状态矩阵。**门是输入的函数，不是状态的函数。** 顺序是"先从输入算出所有门，再去操作记忆"，不是"先看记忆再算门"。

这一点很重要，因为正是它让分块并行成为可能 —— 所有门都可以在整个序列上一次性批量算完，不用等递归。

三个门各自的算法：

**遗忘门。** 走低秩：7168 先压到 128，再展开到 12288，每个头拿到 128 个数。为什么绕这一下？因为直接用 7168 乘 12288 的满矩阵要八千八百万参数，太贵；低秩之后一百八十万。然后这 128 个数和每个头一个的可学习参数、以及一个偏置结合，最后取指数变成 0 到 1 之间的衰减率。K3 还在 log 空间给它加了个下限负 5，不许一步衰减太狠。

**beta，写入强度。** 最简单：7168 直接映射到 96，每个头一个数，过 sigmoid。它决定这次改写是彻底覆盖还是轻微修正。

**输出门。** 7168 直接映射到 12288，**满秩**，配置里 use full rank gate 就是指这个。它是三个门里最贵的，八千八百万参数。它不参与状态更新，只在最后一步逐维度调节读出来的东西放多大声。

有意思的是这个粒度差异：遗忘门和输出门都是每通道 128 个数的细粒度，唯独 beta 是每头一个标量。想想也合理 —— 遗忘和输出是"哪些维度要保留、哪些要放大"，天然是逐维度的事；而"这个 token 值不值得写进记忆"更像是一个整体判断。

---

顺便说一句，你刚才那个类比其实还漏了一个可以自己想的点：既然门是从输入算的、和状态无关，那模型判断"要不要忘、忘多少"的时候，它**看不见自己记了什么**。它只能根据当前读到的内容来决定。这算是个限制 —— 有点像你记笔记时不翻回前面，只凭当前听到的话决定要不要划重点。

要不要接着往下走？可以讲分块并行怎么把递归摊平成矩阵乘法，那是让它在 GPU 上真正跑得快的关键；或者讲那二十四层 MLA 怎么和 KDA 配合、为什么敢不用位置编码。

## 👤 用户

我们来讲一下你刚刚说的那个达塔 rule 我好像没有听到哪里有达塔的地方呀，对。就你说的那个精确改写，但听起来我们还是加上去和遗忘，好像也没有精确改写的地方

## 🤖 Claude

问得好，这说明你在认真跟。它确实在，而且就藏在**一个减号**里 —— 我上一轮把它讲快了。

回到那三步。第二步我说的是"查旧值"，第三步我说的是"用真实的 V 减去这个旧值"。

**那个减法，就是 delta。**

参考实现里那一行，写成一个式子就是：状态矩阵加上，k 和"v 减去 k 查出来的旧值"的外积，再乘 beta。括号里那个减法就是全部的秘密。朴素线性注意力那一行里没有减号，直接是 k 和 v 的外积。**两者的差别只有这一处。**

delta 这个词来自经典神经网络里的 Widrow-Hoff delta rule，delta 就是误差：**你想要的，减去你现在实际有的。**

---

**现在我给你算一遍，你就会看到"精确改写"不是个比喻，是真的。**

假设 beta 等于 1，k 已经归一化成单位长度。

第一步，拿 k 去查当前的状态矩阵，得到旧值，假设查出来是"北京"。

第二步，误差等于"上海"减"北京"。

第三步，把 k 和这个误差的外积加进去。

**现在关键的地方来了 —— 更新完之后，你立刻再拿同一个 k 去查这张表，会查到什么？**

查到的是：旧值"北京"，加上 k 和 k 的点积乘以那个误差。而 k 是单位向量，k 点 k 等于 1。所以是"北京"加上"上海减北京"。

**北京被减掉了。剩下的正好是"上海"，一分不多一分不少。**

这就是精确改写。不是让北京慢慢褪色，是当场抵消掉。beta 等于 1 就是完全覆盖，beta 等于 0 就是完全不动，中间就是在旧值和新值之间做插值。beta 的语义干净成这样，**全靠 k 是单位长度** —— 这就是我前面说的那个 L2 归一化，它的意义在这里才兑现。

---

**那别的记忆会被误伤吗？**

不会，或者说基本不会。因为这次更新加进去的东西，是 k 这个方向上的外积。你拿另一个键 k' 去查的时候，取回的误差量要乘以 k' 和 k 的点积。**如果 k' 和 k 大致垂直，这个点积接近零，那条记忆纹丝不动。**

所以这个改写是**有方向性的**：只擦掉"张三"这个方向上的旧记录，别的键各占各的地盘，互不干扰。128 维空间里能塞下很多互相接近垂直的方向，这就是这张表的容量来源。

---

**对比一下没有减号会怎样。**

朴素做法直接加 k 和"上海"的外积。那你再查"张三"，得到的是"北京加上海"两个向量叠在一起。模型只能看到一团混合物，不知道哪个是最新的。而且你越写，糊得越厉害 —— 这就是朴素线性注意力在长文本上崩掉的根本原因。

**所以现在遗忘有两条独立的路径**，这点我希望你分清楚：

一条是衰减，被动的、跟内容无关的整体褪色，逐通道控制速率。

另一条是 delta 的覆盖，主动的、精确定向的擦除，只针对被冲突的那个键。

这也回答了你上一轮的担心 —— 九十万 token 前的用户 prompt 之所以还可能在，靠的主要不是"衰减得慢"，而是"**没人拿相似的键来覆盖它**"。

---

再考你一个，答上来 delta rule 就彻底是你的了：

**如果同一个 k 连续来两次，v 也完全一样，beta 都等于 1。第一次写入之后，你觉得第二次那一步实际上会往状态矩阵里加进去多少东西？**

## 👤 用户

我想一下，这里我其实没有太理解，那可能同时来两个，他们相加的那块会有什么吗？还是没有太理解对。

## 🤖 Claude

没关系，这个我直接给你算，答案挺漂亮的。

第一次写入之后，表里"k 这个键"对应的值，已经精确地等于 v 了 —— 这是刚才那笔账的结论。

现在第二次，同样的 k 来了，同样的 v。三步走：

第一步，拿 k 去查表。查出来是什么？**是 v。** 因为上一步刚写进去。

第二步，算误差。**v 减 v，等于零。**

第三步，把 k 和零向量的外积加进去。零和任何东西的外积都是零矩阵。

**所以第二次什么都没加。状态矩阵纹丝不动。**

---

这个性质叫**幂等**：同一条信息写一遍和写十遍，效果完全一样。

而这正是我想让你看到的、delta rule 和朴素做法最本质的差别。朴素做法是"加"，同一句话重复十遍，那个方向上的记忆就被放大十倍，把别的记忆全压下去了。而 delta rule 是"**让表和事实对齐**"，已经对齐了就不动，它写的不是次数，是状态。

你现在应该能感觉到那个视角切换了：**它不是在记流水账，它是在维护一张始终和当前事实保持一致的表。** 新信息来了就更新对应条目，冗余信息来了就自动忽略。

所以模型里那些高频重复的东西 —— 标点、常见词、模板句 —— 第一次写进去之后，后面基本就不再占用写入带宽了。记忆容量自动让给了真正带新信息的 token。

---

补一句诚实的：实际情况没有这么干净。两次之间夹着衰减，那个 k 查出来的会是稍微淡一点的 v，所以误差不是严格的零，而是一个很小的补偿量 —— 相当于**把被衰减磨掉的那一点点补回来**。

这反而更妙：**一条信息只要不断被重复提及，就能一直被补回满值，永远不会因为衰减而消失。** 用得越频繁的记忆活得越久。这个行为完全是从那个减号里自然长出来的，没人专门设计过。

---

到这儿 delta rule 你已经通了。要不要换个方向？我建议下一个讲**分块并行** —— 就是 prefill 的时候，几十万个 token 同时进来，怎么把这个一步一步的递归摊平成 GPU 喜欢的大矩阵乘法。它是这套东西能真跑起来的另一半，思路也很巧。或者你想先歇一下、或者回头再捋捋前面哪块，都行。

## 👤 用户

OK，来讲一讲 prefill 吧。我先来自己设想一下，你刚刚说可以分块并行，对吧？那也就是说其实我没有想通，我还是觉得它是类似一个 RNN，它是怎么能够分块并行的呢？还是感觉有些怪。

## 🤖 Claude

你的怀疑很对，而且这正是该卡的地方。RNN 不能并行是常识 —— 因为第一百步要等第九十九步算完。

但 KDA 能，原因就一个字：**线性**。

---

**先说清楚普通 RNN 为什么不行**

普通 RNN 大概是：新状态等于，激活函数作用在（旧状态乘一个矩阵，加上新输入）。

**卡死在那个激活函数上。** 有了它，你没法把两步合并成一步 —— tanh 套 tanh 展不开，只能老老实实一步一步算。

**KDA 的递归里没有激活函数。**

回想那三步：状态先逐行乘衰减，然后减去 k 查出来的旧值，再加上外积。**从头到尾，状态只经历了乘常数和加法。没有任何非线性。**

也就是说，"从旧状态到新状态"这件事，是一个**线性映射**，说白了就是一个矩阵。而且这个矩阵长什么样，只取决于当前 token 的 k、衰减、beta —— **这些都是从输入直接算出来的，不依赖状态**。

这就是我前面强调"门是输入的函数，不是状态的函数"的回报，它在这里兑现。

---

**线性带来什么**

线性映射可以**结合**。

第一步是映射 A，第二步是映射 B，第三步是 C。你要算 C 作用在 B 作用在 A 作用在初始状态上。

因为可结合，你完全可以**先把 A、B、C 三个映射自己乘在一起**，得到一个合成映射，再一次性作用在初始状态上。

**而"把 A、B、C 乘起来"这件事，是不需要知道初始状态的，所以可以并行做。** 这就是口子。

---

**但直接这么干太贵**

每个映射是 128 乘 128 的矩阵。你要把六十万个这样的矩阵连乘起来，成本比老老实实递归还高。

所以真正的做法是**分块**，序列切成 64 个 token 一块。而且要利用一个关键结构：**每一步的那个映射不是任意矩阵，它是单位矩阵减去一个秩一的东西**，因为 delta 更新本质上就是一个方向上的投影。一堆秩一修正连乘，有紧凑的闭式表达，不用真的去乘 128 乘 128 的矩阵。

---

**现在讲块内到底怎么算，这是最漂亮的部分**

对第 i 块里的某个 token，它的输出可以**干净地拆成两项相加**：

**第一项：来自块开头那个状态矩阵的贡献。** 这一项，块里 64 个 token 全都查同一张表 —— 因为这张表在整块开始前就固定了。**64 个查询查同一张表，那就是一个 64 乘 128 的矩阵，乘一个 128 乘 128 的矩阵。一次大矩阵乘法搞定，完美并行。**

**第二项：来自同一块内、排在它前面那些 token 的贡献。**

这一项特别有意思。它的形式是：块内的 Q 乘块内的 K 的转置，得到一个 64 乘 64 的分数矩阵，加一个因果掩码（只能看前面），然后乘上块内的 V。

**你发现了吗 —— 这就是标准注意力。** 一模一样的形状，只是没有 softmax。

所以块内走的是注意力的路子，块间走的是递归的路子。**KDA 在 prefill 时的真面目，是一个 64 乘 64 的小窗口注意力，配上一个在窗口之间传递的状态矩阵。**

算完一块，把这块 64 个 token 的信息一次性压进状态矩阵，传给下一块。

---

**还有一个坎：块内的 delta 怎么办？**

按定义，块里第 30 个 token 做 delta 改写时，要查的是"第 29 个 token 更新完之后的状态"。可我们说好了整块并行，中间状态根本没算出来。

解决办法是：把块内这 64 个 token 之间的相互影响，写成一个 **64 乘 64 的下三角方程组**，一次性解出来。因为是三角的，解起来很快，而且全是矩阵运算。

参考实现里那一段循环，做的就是这件事 —— 先构造 64 乘 64 的矩阵，然后把它逆过来。这个技巧有个名字叫 WY 表示，最早来自数值线性代数里做 Householder 变换的连乘。

**解出来之后，得到两个修正过的量，代码里叫 w 和 u。** 你可以理解成：把"每个 token 应该写入什么"这件事，在考虑了块内所有互相覆盖之后，一次性算清楚了。之后就全是大矩阵乘法。

---

**账算一下**

一百万 token，切成一万五千多块。递归只走一万五千步，而不是一百万步。

而每一步内部，是 64 乘 128 这个量级的矩阵在乘 —— GPU 吃得很饱。相比之下逐 token 递归是矩阵乘向量，GPU 利用率个位数百分比。

**块为什么选 64？** 太小则递归步数多、并行度不够；太大则那个块内的三角求解是 64 的平方级增长，会开始变贵。64 是个平衡点，也正好对上 GPU 的 tile 尺寸。

**最关键的一点：分块和逐 token 递归，数学上完全等价。** 结果一个字节都不差，只是计算顺序不同。所以 prefill 用分块、decode 用递归，两者无缝衔接 —— prefill 结束时交出来的那个状态矩阵，就是 decode 的起点。

---

到这儿其实有个挺深的观感：**分块并行揭示了线性注意力和标准注意力不是两个东西，是同一个东西的两端。** 块大小设成 1 就是纯 RNN，块大小设成整个序列长度，就退化成一个没有 softmax 的全注意力。KDA 只是把这个旋钮拧到了 64。

要不要接着讲那 24 层 MLA 怎么和 KDA 配合、为什么敢完全不用位置编码？
